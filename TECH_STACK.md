# 技术栈定稿 — 中心化控制型聊天系统

> 一个"主人 ↔ 多用户"的中心化聊天系统:服务端为唯一真相源,主人可管理全部会话/记录/权限,用户只聊天且本地零留存。本文件汇总此前讨论确定的全部技术选型,作为开发基线。

## 0. 架构原则与不变量(不可违反)

- **中心化,单一真相源在服务端**;主人通过自己控制的云服务器掌控一切。
- **API-first 无头核心**:后端不含任何 UI,只暴露契约。
- **前后端分离**:后端独立仓 + 前端 monorepo(共享 SDK + 各客户端)。
- **模块化**:NestJS 模块 + 内部事件总线;新功能 = 新挂一个订阅事件的模块,内核不动。
- **权限服务端强制**:每个动作服务端校验;API 按角色分面(用户面受限、管理面特权),绝不靠"客户端没按钮"来挡。
- **消息只持久化在服务端;所有客户端内存态**:会话期间在内存、App 杀/断线即清、重启重新登录从服务器拉;零本地留存、不可导出。
- **管理员能力**:增删改任意聊天记录、封禁用户/IP、查看用户 IP 属地。

## 1. 后端

| 关注点 | 选型 | 备注 |
|---|---|---|
| 语言 / 框架 | **TypeScript + NestJS** | 模块/DI/Guard 的强结构防技术债;Guard 直接落"权限服务端强制";与全栈同语言。(嫌重可换 Fastify;勿用裸 Express) |
| 实时通信 | **Socket.IO + Redis adapter** | 房间/重连/横向扩展现成;Socket.IO 绑定 TS 也是选 NestJS 的原因之一 |
| 数据库 | **PostgreSQL** | 唯一消息存储 |
| 缓存 / 在线状态 / pub-sub | **Redis** | 兼作 Socket.IO 多实例适配 |
| 对象存储 | **MinIO**(S3 API) | 图片/语音/附件;可平滑换 S3/R2 |
| 语音通话 | **LiveKit**(SFU,纯语音) | 服务端路由 = 可控 + 用户间不暴露 IP;**视频已砍** |
| 鉴权 | **JWT(access+refresh)+ RBAC + 用户级权限位** | master 由环境变量初始化,无公开注册 |
| IP 属地 | **ip2region(国内省市)+ MaxMind GeoLite2(全球)** | 需定期更新数据库 |
| 审计 | **只追加审计日志** | 记录管理员所有操作(改记录、封禁等) |
| API 契约 | **OpenAPI(REST)+ 类型化 WebSocket 事件 schema** | 契约先行 → codegen 客户端,两端自动同步 |

## 2. 前端(前后端分离)

仓库:**后端独立仓 + 前端 monorepo**。monorepo 内含一个共享 **TS core SDK**(协议类型、API client、WS client),供所有客户端复用。**管理端与用户端是两套独立 App,用户端不含任何管理代码。**

| 客户端 | 选型 | 状态 / 备注 |
|---|---|---|
| 管理端(Web 优先) | **Refine**(后台 CRUD/权限/封禁/IP)+ **chatscope**(实时聊天面板)+ **Leaflet + OpenStreetMap**(IP 属地地图) | Refine 管"管理"那半,聊天面板嵌 chatscope |
| 用户端 · 移动(iOS/Android) | **React Native + react-native-gifted-chat** | **原生优先**;内存态;防截图(安卓 FLAG_SECURE / iOS 安全层取巧 + 检测);图片按需点开拉取、不可保存 |
| 用户端 · Web | chatscope + 内存态 | **暂缓**,后续单独设计;Web 天生防不住截图/审查,届时给更受限功能集或接受较高泄露面 |

## 3. 推送(可插拔)

统一 **NotificationProvider** 接口,**只推新消息**。分层通道:

- **WebSocket 前台**(App 打开):即时、免费、全平台。
- **Web Push(VAPID)**:Web/PWA 后台。
- **APNs**:iOS 后台(只需苹果开发者账号)。
- **FCM**:安卓后台(非中国大陆;国内场景退回前台 Service 保活或厂商推送)。

> 这是全盘最难啃的运维点(尤其 iOS 后台 + 自托管),工期和预期要给够。

## 4. 数据控制机制

- **全端内存态**:不在任何用户设备上持久化聊天记录;每次会话从服务器拉(游标分页 + 懒加载)。
- **不可导出/下载**:无导出入口;媒体走**短时效签名 URL + `no-store`**;图片**按需点开**才从服务器拉、用完不落本地。
- **防截图**:安卓 **FLAG_SECURE**(彻底禁截屏/录屏);iOS 取巧(安全层 hack,打折)。
- **服务端是唯一闸门**。**物理上限**:App 要能解密显示,故 root/越狱 + 注入设备上可运行时 dump 明文,拍屏也挡不住 → 用户端**原生为主**。

## 5. 部署与基础设施

- **编排**:一份 **Docker Compose**(NestJS app、PostgreSQL、Redis、MinIO、LiveKit、Nginx + TLS/Let's Encrypt),一键起。
- **服务器**:云服务器常驻;**起步 4c4g 够用,推荐 4c8g**(内存是瓶颈,给 Postgres 缓存留余量)。
- **磁盘**:单独规划(MinIO 媒体累积 + Postgres 增长 + 备份)。
- **备份**:DB + MinIO **加密备份 + 恢复演练**(单机无冗余)。
- **CI/CD**:GitHub Actions(lint → typecheck → test〔重点覆盖鉴权与协议〕→ build → docker build)。

## 6. 参考项目(学架构/设计,**不抄代码**)

- **Tinode** — 精简自托管 IM 核心架构(协议、在线、权限)。
- **Chatwoot** — 客服产品形态与 UX。
- **Telegram 频道管理员模型** — 权限/封禁/审计的概念(其服务端闭源,只借概念)。
- 直接作为依赖使用:LiveKit、ip2region、GeoLite2、Refine、chatscope、react-native-gifted-chat。

## 7. 明确砍掉 / 暂缓

- **视频通话**:砍掉,只保留语音 —— 顺带消除单机最大资源风险。
- **Web 用户端**:暂缓,后续单独设计。
- **本地加密缓存**:不做 —— 数据量小(以文字为主、图片按需),内存态更简单/更安全/更一致;若历史变大或需离线再议。
- **微服务 / 集群 / k8s**:不做 —— 几百用户规模用不上;模块化单体 + 单机 Compose 足够。
